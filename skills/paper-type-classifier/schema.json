{
  "type": "object",
  "required": ["paperType", "confidence", "characteristics", "priorities"],
  "properties": {
    "paperType": {"enum": ["method", "theory", "empirical", "survey", "system", "dataset"]},
    "confidence": {"type": "number", "minimum": 0, "maximum": 1},
    "characteristics": {"type": "object"},
    "priorities": {"type": "array", "items": {"type": "string"}}
  }
}
